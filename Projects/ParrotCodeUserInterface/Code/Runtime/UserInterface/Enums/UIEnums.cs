namespace ParrotCode.UI
{
    public enum State
    {
        Normal,
        Hovered,
        Pressed,
        Selected,
        Disabled
    }

    public enum Theme
    {
        Default,
        LightMode,
        DarkMode
    }

    public enum Navigation
    {
        None,
        Vertical,
        Horizontal,
        Automatic,
        Explicit
    }
}

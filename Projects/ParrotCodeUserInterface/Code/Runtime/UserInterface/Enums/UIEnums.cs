namespace ParrotCode.UI
{
    public enum UIStateType
    {
        Normal,
        Hovered,
        Pressed,
        Selected,
        Disabled
    }

    public enum ThemeType
    {
        Default,
        LightMode,
        DarkMode
    }
}
